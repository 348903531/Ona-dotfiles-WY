# Never commit secrets or local-only artifacts into dotfiles.
*.local
*.local.*
.env
.env.*
*token*
*secret*
*credential*
*.pem
*.key
id_rsa*
google_token.json

# Python 产物（sync-hooks.sh 跑 py_compile 会生成）
__pycache__/
*.pyc
